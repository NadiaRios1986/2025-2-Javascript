
const { ask } = require('../helpers/input'); //aqui van las funciones

function nombresVocal(nombres) {
    const vocales = ["a", "e", "i", "o", "u"]
    for (let a=0; a<nombres2.length; a++){
        const primeraletra = (nombres2[a][0].tolowercase);
      }

}
    

async function main() {//ejecuta todo principal funcion principal 
  const personas = [ "Alma", "Pedro", "Olga", "Angel"]
 

  for (let a=0; a<nombres2.length; a++){
    const primeraletra = (nombres2[a][0].tolowercase);
  }
}

main(); // ejecuta la funcion que se llama  main
